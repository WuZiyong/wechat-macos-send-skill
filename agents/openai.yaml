interface:
  display_name: "WeChat Fast Sender"
  short_description: "Send one verified WeChat text, image, or file"
  default_prompt: "Use $wechat-macos-send to send this one WeChat text, image, or file from my Mac with a single verified send call."
